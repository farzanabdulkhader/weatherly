import AirIcon from "@mui/icons-material/Air";
const iconStyle = {
  fontSize: "4.5rem",
  marginRight: "1rem",
  marginTop: "0.8rem",
  display: "inline-block",
};

export default function Logo() {
  return (
    <span>
      <AirIcon style={iconStyle} />
    </span>
  );
}
